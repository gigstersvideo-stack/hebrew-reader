# קורא — Hebrew Reader

Read-along инструмент для изучения иврита через чтение книг: клик по
слову — перевод и разбор, клик по предложению — литературный перевод,
аудио подсвечивает слова по ходу воспроизведения. Один файл
(`reader-prototype.html`), без сервера и без сборки — открывается прямо
в браузере.

**Демо:** https://gigstersvideo-stack.github.io/hebrew-reader/reader-prototype.html

## Что умеет

- Клик по слову → лемма, часть речи, перевод, подробный разбор, корень.
- Клик по предложению → дословный (слово-в-слово) и литературный перевод.
- Аудио по предложениям с подсветкой текущего слова в реальном времени;
  сегментированная шкала прогресса — клик по сегменту сразу играет то
  предложение.
- Личный словарь: сохранение слов, флэш-карты для тренировки, экспорт
  в Anki, частотный список слов книги с "процентом покрытия" известной
  лексики.
- Иллюстрации и мнемоника для отдельных слов.
- Пагинация, автопрокрутка, закладка последней открытой страницы.
- Редактирование текста прямо на сайте (без сервера — правки
  скачиваются файлом).

Полная история изменений — в [CHANGELOG.md](CHANGELOG.md).

## Движок vs книги — важное разделение

`reader-prototype.html` ничего не знает про конкретные книги: он умеет
только читать формат `book-data.json` (текст, переводы, тайминги слов)
и, если есть, аудио к нему. Сам движок и встроенный демо-текст —
оригинальные, без ограничений, публикуются свободно.

**Любая добавленная книга — ответственность того, кто её добавил.**
Есть три способа подключить книгу, у каждого свой уровень приватности:

1. **`books-manifest.json`** — книги, которые видны всем посетителям
   сайта. Подходит только для текстов без ограничений по авторским
   правам (как встроенный демо-текст).
2. **"Добавить свою книгу"** прямо в интерфейсе — файл или вставленный
   JSON проверяется и сохраняется в `localStorage` браузера, никуда не
   отправляется. Подходит для собственных материалов, которыми не
   планируешь делиться.
3. **Прямая ссылка** `reader-prototype.html?book=<URL>&title=...` —
   книга подгружается с любого адреса, который ты укажешь (например,
   приватный хостинг с непредсказуемым адресом). Ничего не сохраняется
   на самом сайте — только временно в памяти открытой вкладки.

## Формат book-data.json

```json
{
  "title": "Необязательно — подставится в поле «Название книги» при импорте",
  "sentences": [
    {
      "id": "s1",
      "literal": "дословный — перевод — слово — за — словом",
      "fluent": "Литературный перевод предложения.",
      "audio": "audio/s1.mp3",
      "words": [
        { "t": "מילה", "lemma": "מִלָּה", "pos": "сущ.", "tr": "слово",
          "extra": "доп. разбор", "root": "מ-ל-ל", "start": 0.1, "end": 0.5 }
      ]
    }
  ]
}
```

`audio` и `start`/`end` у слов — необязательны: без них предложение
всё равно читается, просто подсветка идёт по грубой оценке времени
вместо реальных таймкодов.

## Как написать новую историю без авторских прав под нужный уровень

`2_generate_story.py` генерирует оригинальный рассказ на иврите под
уровень ульпана (алеф/бет/гимель/далет) через бесплатный Gemini API —
structured output гарантирует валидный JSON, отдельный вызов того же
API расставляет огласовки (никуд):

```bash
export GEMINI_API_KEY=твой_ключ  # бесплатно: https://aistudio.google.com/apikey
python 2_generate_story.py alef "робот-уборщик находит на балконе кота" --out story-alef.txt
python 5_gemini_pipeline.py story-alef.txt --out book-data-alef.json
```

По умолчанию история пишется одним запросом к API — этого достаточно
для типичных объёмов уровня (алеф ~150 слов, ... далет ~800). Для книги
заметно больше — `--chapter-words` переключает генерацию на главы: каждая
пишется отдельным запросом с кратким `summary_so_far` и хвостом предыдущей
главы в контексте (а не всей книгой целиком), огласовки расставляются по
главе сразу после неё, прогресс сохраняется после каждой главы:

```bash
python 2_generate_story.py gimel "экспедиция ищет пропавший корабль в Средиземном море" \
  --word-count 2500 --chapter-words 400 --out story-gimel.txt
# если прервалось (кончилась дневная квота, сеть моргнула) — тем же --out:
python 2_generate_story.py gimel "..." --word-count 2500 --chapter-words 400 \
  --out story-gimel.txt --resume
```

## Как добавить иллюстрации-мнемоники к готовой книге

`illustrate_book.py` сам находит 2-3 самых редких/значимых слова готового
`book-data.json` (сверка с настоящими частотными списками иврита от
Dicta — не наугад), пишет для каждого мнемонику (созвучие + абсурдная
сценка) через Gemini и рисует картинку настоящей диффузионной моделью
через Cloudflare Workers AI (`@cf/black-forest-labs/flux-1-schnell`,
бесплатно в рамках дневного лимита) — SVG для мнемоник не годится, он не
умеет достоверно рисовать конкретное действие/сценку:

```bash
export GEMINI_API_KEY=твой_ключ
export CF_ACCOUNT_ID=id_твоего_аккаунта_cloudflare
export CF_API_TOKEN=токен_с_правом_Workers_AI
python illustrate_book.py book-data.json --images-dir illustrations_mybook --count 3
```

## Как озвучить книгу без готовой аудиокниги

`3_generate_audio.py` озвучивает уже размеченный `book-data.json`
через бесплатный [edge-tts](https://github.com/rany2/edge-tts) —
тайминги слов берутся прямо из событий синтеза речи, без Whisper и без
отдельного шага выравнивания:

```bash
python 3_generate_audio.py book-data.json book-data-voiced.json audio_dir
```

## Баг-репорты

Кнопка 🐞 в интерфейсе открывает предзаполненный GitHub Issue с
диагностикой (браузер, страница, время) — либо создай Issue вручную.
